# Zero outside cloud: GitHub-hosted compiler objects + registry cache.
# No CARGO_TOG_BUCKET secrets required.

- uses: your-org/cargo-tog/action@main
  with:
    mode: github
    key: test-${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}
    install-nextest: "true"

# Even lighter — downloads only, no object-cache engine:
# - uses: your-org/cargo-tog/action@main
#   with:
#     mode: registry-only
#     key: test-${{ runner.os }}-${{ runner.arch }}
