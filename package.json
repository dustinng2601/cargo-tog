{
  "name": "cargo-tog",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise Cargo build-cache coordination for monorepos and polyrepos",
  "type": "module",
  "bin": {
    "cargo-tog": "./scripts/cargo-tog.mjs"
  },
  "scripts": {
    "build": "cargo build --release",
    "doctor": "cargo run -q -- doctor",
    "cache-plan": "cargo run -q -- cache-plan",
    "test": "cargo test",
    "test:smoke": "cargo test && cargo run -q -- doctor && cargo run -q -- cache-plan"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
